# Copie este arquivo para "config.py" na raiz do projeto e preencha com os
# valores reais. NUNCA commite o config.py de verdade (já está no .gitignore).
#
# Isso é só o segredo/conta — qual campanha/cliente rodar é escolhido na hora
# de chamar o CLI, com --client clients/<nome>.json.

ACCESS_TOKEN = ""
AD_ACCOUNT_ID = "act_..."
API_VERSION = "v21.0"
