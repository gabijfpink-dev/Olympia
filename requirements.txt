facebook-business>=21.0.0
python-dotenv>=1.0.0
requests>=2.31.0
pandas>=2.0.0
openpyxl>=3.1.0

# promo_bot (opcional: só necessário para o modo `--listen` do Telegram)
telethon>=1.36.0

# promo_bot (opcional: só necessário se for enviar promoções pra grupos do
# WhatsApp — depois de instalar, rode também `playwright install chromium`)
playwright>=1.45.0
