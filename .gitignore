.env
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
output/

# config.py contém o access token real — nunca deve ser commitado.
# (só o da raiz: sem a barra, esse padrão também escondia promo_bot/config.py,
# que é código normal do projeto, não segredo.)
/config.py
*.xlsx
.state/

# Sessao logada do Telegram (promo_bot) - equivale a uma credencial, nunca commitar.
*.session
*.session-journal

# Sessao logada do WhatsApp Web (promo_bot/whatsapp_publisher.py) - idem.
whatsapp_session/
